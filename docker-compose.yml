services:
  dashboard-financas:
    build: .
    ports:
      - "3005:80"
    depends_on:
      - backend
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    env_file:
      - backend/.env
    restart: unless-stopped
